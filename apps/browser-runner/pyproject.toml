[project]
name = "ai-assistant-browser-runner"
version = "0.1.0"
description = "AI Assistant browser runner placeholder"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
